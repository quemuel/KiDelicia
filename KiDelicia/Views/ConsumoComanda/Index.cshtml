@model IEnumerable<KiDelicia.Models.ConsumoComanda>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValorConsumo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dataHoraLancamento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValorConsumo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dataHoraLancamento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdConsumoComanda }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdConsumoComanda }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdConsumoComanda })
        </td>
    </tr>
}

</table>
